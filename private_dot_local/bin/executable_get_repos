#! /usr/bin/bash

repos=(amateurfunk minirack start qmk_firmware cantp falkkyburz.github.io)
local=~/dev

for repo in "${repos[@]}"; do
if [[ ! -d ${local}/${repo} ]]; then
  git clone "https://github.com/falkkyburz/${repo}.git" ${local}/${repo}
fi
done
